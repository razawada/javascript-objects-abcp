var playlist = {
  Marina: "Blue",
  MJ: "Thriller",
  MIA: "Powa",
};

function updatePlaylist(playlist, artistName, songTitle) {
  obj[key] = value;

  return obj;
};

function removeFromPlaylist(obj, key) {
  delete obj.key;
  return obj;
};
